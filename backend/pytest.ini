[pytest]
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    unit: Unit tests (no infrastructure)
    integration: Integration tests (require PostgreSQL)
    api: API endpoint tests
    service: Service tests with mocked HTTP
